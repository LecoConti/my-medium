---
layout: layouts/base.njk
title: Home
description: "Latest thinking from the My Medium Prototype team."
---
{% from "components/article-card.njk" import articleCard %}
<section class="feed">
  <header class="feed__header">
    <h1 class="feed__title">Latest Articles</h1>
    <p class="feed__subtitle">Fresh perspectives from the team.</p>
  </header>
  {% if collections.articles | length %}
    <div class="feed__grid">
      {% for article in collections.articles %}
        {% set author = collections.authors | selectattr('data.handle', 'equalto', article.data['author-handle']) | first %}
        {% set publication = collections.publications | selectattr('data.slug', 'equalto', article.data.publication) | first %}
        {{ articleCard(article, author, publication) | safe }}
      {% endfor %}
    </div>
  {% else %}
    <p>No articles published yet. Add Markdown files under <code>content/articles/</code>.</p>
  {% endif %}
</section>

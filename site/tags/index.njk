---
layout: layouts/base.njk
title: Tags
description: "Browse articles by topic."
permalink: "/tags/"
---
<section class="tag-directory">
  <header class="tag-directory__header">
    <h1>Tags</h1>
    <p>Explore topics covered in the publication.</p>
  </header>
  {% if collections.tags | length %}
    <ul class="tag-directory__list">
      {% for tag in collections.tags %}
        <li class="tag-directory__item">
          <a href="/tags/{{ tag.slug }}/">
            <strong>#{{ tag.tag }}</strong>
            <span>{{ tag.items | length }} article{{ 's' if tag.items | length != 1 }}</span>
          </a>
        </li>
      {% endfor %}
    </ul>
  {% else %}
    <p>No tags yet.</p>
  {% endif %}
</section>
